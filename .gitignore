# 仓库里只放游戏本身（*.html）。
# 下面这些是过程的副产品 —— 本地留着有用，进仓库只会让历史和体积变脏。
# 想放进去：注释掉对应那行，或者单独强加 `git add -f 路径`。

# 调平衡的测试脚本（_t.mjs _bal.mjs _pick.mjs …）、预览页、截图、旧备份
_*
_tmp_*.pdf

# 爬来的第三方资料（网页原文、统计年鉴 PDF）—— 别人的东西，不适合传到公开仓库
_research/
.research/
